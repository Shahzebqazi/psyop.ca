apiVersion: apps/v1
kind: Deployment
metadata:
  name: psyop-website
  namespace: default
  labels:
    app: psyop-website
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: psyop-website
  template:
    metadata:
      labels:
        app: psyop-website
        version: v1
    spec:
      containers:
      - name: psyop-website
        image: psyop/psyop-website:latest
        ports:
        - containerPort: 8080
          protocol: TCP
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        env:
        - name: ENV
          value: "production"
      restartPolicy: Always
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
